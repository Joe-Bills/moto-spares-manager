{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate --settings=moto_spares_manager.settings_railway && python manage.py collectstatic --noinput --settings=moto_spares_manager.settings_railway && DJANGO_SETTINGS_MODULE=moto_spares_manager.settings_railway gunicorn moto_spares_manager.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
